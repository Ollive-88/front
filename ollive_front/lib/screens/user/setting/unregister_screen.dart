import 'package:flutter/material.dart';

class UnregisterScreen extends StatelessWidget {
  const UnregisterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('자취생활의 모든 것, Ollive'),
        centerTitle: true,
      ),
      body: const Column(
        children: [Text('hihi')],
      ),
    );
  }
}
